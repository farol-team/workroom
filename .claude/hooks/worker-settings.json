{
  "_comment": "flow worker guardrails. /flow-run Phase 1 copies this file into each card worktree as .claude/settings.local.json (settings.local.json is the per-checkout layer; it never gets committed). The hook scripts are copied alongside into .claude/hooks/.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/scope-guard.sh"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/git-guard.sh"
          }
        ]
      }
    ]
  }
}
