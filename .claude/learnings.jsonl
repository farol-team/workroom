{"date":"2026-08-01","card":"i131","type":"pitfall","key":"catalog-specs-must-pin-literals","insight":"Specs over data modules in `desktop/src` are stub-passable whenever the expected value is read back out of the module under test (`toBe(profile.cliInstall.unix)`), so catalog contents must be asserted as literal strings.","confidence":8,"files":["desktop/test/catalog.test.ts"]}
{"date":"2026-08-01","card":"i131","type":"pitfall","key":"tauri-path-resolution-untestable-without-mock-app","insight":"Functions in `desktop/src-tauri/src/lib.rs` that take `&AppHandle` to read `app.path()` cannot be exercised by any spec because `tauri` is declared with `features = []` in Cargo.toml (no `test` feature, so no `mock_app`), so a plan that asserts resolution-order behavior must require the candidate directory list be injectable — otherwise the specs silently degrade into testing an array the test itself built.","confidence":9,"files":["desktop/src-tauri/src/lib.rs","desktop/src-tauri/Cargo.toml"]}
{"date":"2026-08-01","card":"i131","type":"pitfall","key":"desktop-has-no-dom-tests","insight":"The desktop package's `pnpm test` covers only pure logic (`test/rules.test.ts`, `test/catalog.test.ts`) and vite.config has no jsdom/happy-dom environment, so any plan touching `desktop/src/main.ts` or `desktop/index.html` must add a manual verification step — a UI control can be deleted with every gate green.","confidence":9,"files":["desktop/vite.config.ts","desktop/test/rules.test.ts","desktop/src/main.ts"]}
{"date":"2026-08-01","card":"i131","type":"pitfall","key":"desktop-webview-has-no-csp","insight":"The Tauri config sets `\"csp\": null` and main.ts builds several elements via `innerHTML`, so any new `#[tauri::command]` that accepts a free-form string and executes it (shell, path, URL) hands the webview that authority directly — prefer commands that take an identifier and resolve the dangerous value Rust-side.","confidence":7,"files":["desktop/src-tauri/tauri.conf.json","desktop/src-tauri/src/lib.rs","desktop/src/main.ts"]}
{"date":"2026-08-02","card":"pending-card1","type":"pitfall","key":"active-storage-test-disk-persists-between-runs","insight":"The test Active Storage service is Disk rooted at `tmp/storage` with no teardown anywhere in `test/test_helper.rb`, so objects survive between suite runs — any spec asserting \"uploaded once\" or \"not present\" must generate fresh per-run content or it passes on the first run and fails (or falsely passes) on the next.","confidence":9,"files":["server/config/storage.yml","server/test/test_helper.rb","server/test/services/record_store/objects_test.rb"]}
{"date":"2026-08-02","card":"pending-card2","type":"pitfall","key":"readonly-rows-reject-dependent-destroy","insight":"This codebase's append-only pattern (`def readonly? = persisted?`, as in Activity) makes `has_many ..., dependent: :destroy` raise ActiveRecord::ReadOnlyRecord when the parent is destroyed — append-only children must cascade via `dependent: :delete_all` or the FK.","confidence":9,"files":["server/app/models/activity.rb","server/test/models/channel_record_test.rb"]}
{"date":"2026-08-02","card":"i180","type":"pitfall","key":"worktree-branch-tracks-origin-main","insight":"A worktree branch created from `origin/main` tracks it; with `push.default=upstream` a bare `git push -u origin <branch>` fast-forwards main itself — set push.default=simple repo-wide and unset the stale upstream before any worker pushes.","confidence":9,"files":[".claude/tracker.json","bin/workflow-kit-sync"]}
{"date":"2026-08-02","card":"pending-card2","type":"pattern","key":"room-table-composite-workspace-fk","insight":"Any new table carrying both workspace_id and a parent id (channel_id, agent_session_id, agent_run_id) must get a composite foreign key `column: [<parent>_id, workspace_id], primary_key: [id, workspace_id]` in addition to the plain FK — this is the database-level guard that a child of one workspace cannot reference a parent of another, established in 20260801130001_workspace_id_everywhere.rb and carried by all 5 existing room tables, and adding workspace_id to Workspace::Boundary::ROOMS alone does not provide it.","confidence":9,"files":["server/db/migrate/20260801130001_workspace_id_everywhere.rb","server/db/schema.rb","server/app/models/concerns/belongs_to_workspace.rb"]}
{"date":"2026-08-02","card":"meta","type":"pattern","key":"specs-must-drive-production-wiring","insight":"Cards i174/i176/i177/i179 were all critic-rejected on the same shape: specs exercised the plan's new seams (struct-literal agents, free functions, rendered strings) but not the production entry point (launch, commands, bridge actions) — a PLAN must state which production path each spec drives, or the second critic round will reject it.","confidence":9,"files":[".claude/prompts/plan-format.md",".claude/prompts/test-critic.md"]}
{"date":"2026-08-02","card":"i180","type":"pattern","key":"desktop-fire-and-forget-catch","insight":"In desktop/src/main.ts every promise started without being awaited carries an explicit `.catch` (silent `() => {}` for telemetry, `alert(String(err))` for user-facing work), so any new callback handed to api.ts must terminate its own rejection rather than relying on a caller to await it.","confidence":8,"files":["desktop/src/main.ts","desktop/src/api.ts"]}
{"date":"2026-08-02","card":"i183","type":"architecture","key":"client-opens-alphabetically-first-channel","insight":"The client opens `channels[0]` (`desktop/src/main.ts:865`) and the API returns `Channel.order(:name)` (`server/app/controllers/api/v1/channels_controller.rb:7`), so any seed or preview data meant to be seen on a fresh install must live in the alphabetically first channel — `marketing`, not `meetings`.","confidence":9,"files":["desktop/src/main.ts","server/app/controllers/api/v1/channels_controller.rb","server/db/seeds.rb"]}
{"date":"2026-08-02","card":"i183","type":"operational","key":"preview-against-throwaway-database","insight":"`bin/preview` can be run against a throwaway database without disturbing a developer's server on :3000 by exporting `VITE_WORKROOM_SERVER` (baked in by the build it runs), `WORKROOM_SERVER`, `WORKROOM_PREVIEW_PORT` and `WORKROOM_SHOTS` and pointing a second `bin/rails server` at a `DATABASE_URL` of its own — `DATABASE_URL` overrides the `database:` key in `config/database.yml`.","confidence":9,"files":["bin/preview","desktop/src/main.ts","server/config/database.yml"]}
{"date":"2026-08-02","card":"pending-card2","type":"pattern","key":"composite-channel-fk-makes-cascade-inert","insight":"Every channel-child table carries a composite `(channel_id, workspace_id)` foreign key to `channels(id, workspace_id)` that is NO ACTION, and Postgres enforces it before the plain `channel_id` key, so `on_delete: :cascade` on the plain key never fires — the Rails `dependent:` option is the only cascade, and dropping it breaks channel deletion outright.","confidence":9,"files":["server/db/migrate/20260802120001_the_rooms_record_journal.rb","server/app/models/channel.rb","server/test/models/channel_record_test.rb"]}
{"date":"2026-08-02","card":"pending-card2","type":"pitfall","key":"base-controller-wraps-request-in-transaction","insight":"`Api::V1::BaseController` already wraps every request in `ActiveRecord::Base.transaction` via `around_action :within_one_transaction`, so a controller-level rollback spec passes with or without an explicit inner transaction block and cannot on its own evidence that the action wraps anything.","confidence":8,"files":["server/app/controllers/api/v1/base_controller.rb","server/app/controllers/api/v1/messages_controller.rb"]}
{"date":"2026-08-02","card":"i183","type":"tool","key":"preview-against-a-throwaway-server","insight":"`bin/preview` can be pointed at any server without touching the development database by exporting `WORKROOM_SERVER`, `VITE_WORKROOM_SERVER` (the client reads it at build time, and `bin/preview` builds the client itself), `WORKROOM_PREVIEW_PORT` and `WORKROOM_SHOTS` against a Rails server started with a scratch `DATABASE_URL`.","confidence":9,"files":["bin/preview","desktop/src/main.ts","server/config/database.yml"]}
{"date":"2026-08-02","card":"pending-card3","type":"pitfall","key":"record-store-disk-persists-across-test-runs","insight":"The test object store is Active Storage Disk under `server/tmp/storage` with no teardown, so any spec using fixed fixture content finds the object already stored before the request meant to store it — content-addressed specs must randomize their bytes (e.g. `SecureRandom.hex`) or they pass vacuously.","confidence":8,"files":["server/app/services/record_store/objects.rb","server/test/controllers/api/artifacts_controller_test.rb"]}
{"date":"2026-08-02","card":"pending-card4","type":"tool","key":"shared-test-db-across-worktrees","insight":"All `.flow` worktrees share one PostgreSQL test database, so running `bin/rails test` from a worktree whose `db/schema.rb` predates another's migration silently downgrades the schema for every worktree (`unknown attribute 'sha256' for Artifact`); `bin/rails db:test:prepare` in the newer tree restores it.","confidence":9,"files":["server/db/schema.rb","server/config/database.yml"]}
{"date":"2026-08-02","card":"pending-card5","type":"pitfall","key":"minitest-message-helper-collision","insight":"A Minitest fixture helper named `message` overrides Minitest's own `message` method, so every assertion that has to build a failure message raises instead of reporting — name message fixtures `posted`/`said` instead.","confidence":9,"files":["server/test/services/record_store/git_export_test.rb"]}
{"date":"2026-08-02","card":"pending-card5","type":"pitfall","key":"journaled-messages-are-always-human","insight":"Only `messages_controller#create` appends `message.created`; `runs_controller#message` creates agent-authored messages without journaling, so every `message.created` envelope carries MessageSerializer's User branch (with email) and a fixture journaling an agent message would spec a shape the product never produces.","confidence":7,"files":["server/app/controllers/api/v1/runs_controller.rb","server/app/services/message_serializer.rb"]}
{"date":"2026-08-02","card":"pending-card5","type":"pattern","key":"memory-store-current-is-process-wide","insight":"`Memory::Store.current=` pins a process-wide store that `current` never un-resolves, so any spec that swaps the seam must reset it to nil in teardown or the whole suite becomes order-dependent.","confidence":8,"files":["server/test/services/record_store/git_export_test.rb","server/app/services/memory/store.rb"]}
{"date": "2026-08-02", "card": "i191", "type": "pitfall", "key": "ci-seeds-the-test-database", "insight": "CI's db:prepare seeds the test database (a workroom workspace with meetings/marketing channels) while a local db:test:prepare does not, so specs must mint their own slugs/names instead of reusing seed ones — a bare-seed-slug lookup is unambiguous locally and refused in CI.", "confidence": 9, "files": ["server/test/services/record_store/git_export_test.rb", "server/db/seeds.rb", ".github/workflows/server.yml"]}
{"date":"2026-08-02","card":"i182","type":"pitfall","key":"cable-membership-lookup-needs-entered-workspace","insight":"An ActionCable subscription carries no workspace-entered transaction, so any RLS-covered lookup it makes — including `memberships` for an authorization gate — must sit inside `Workspace.entered(current_workspace)` or it reads an empty table and refuses every legitimate subscriber.","confidence":8,"files":["server/app/channels/room_channel.rb","server/test/channels/room_channel_test.rb"]}
{"date":"2026-08-02","card":"i186","type":"pitfall","key":"cable-capture-helpers-do-not-nest","insight":"Every Action Cable capture helper in the server suite installs a singleton `ActionCable.server.broadcast` and tears it down with `remove_method`, so nesting two captures silently discards the outer one — a test that must observe two streams at once has to capture all targets in a single pass, as #186's `targets` helper does.","confidence":6,"files":["server/test/test_helper.rb","server/test/services/broadcast_test.rb"]}
{"date":"2026-08-02","card":"i176","type":"tool","key":"desktop-tsconfig-excludes-tests","insight":"`desktop/tsconfig.json` sets `include: [\"src\"]`, so the `pnpm exec tsc --noEmit` gate never typechecks `desktop/test/**` — specs can carry type errors (missing required deps fields, wrong shapes) that no CI gate catches.","confidence":9,"files":["desktop/tsconfig.json","desktop/test/timeline.test.ts"]}
{"date":"2026-08-02","card":"i176","type":"operational","key":"preview-before-after-baseline","insight":"A byte-identical before/after `bin/preview` comparison is reproducible: `git archive origin/main` into a scratch tree, `pnpm install --frozen-lockfile` there (never symlink node_modules — pnpm aborts with ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY), then run with `WORKROOM_SHOTS`/`WORKROOM_PREVIEW_PORT` overridden against the same :3000 server.","confidence":8,"files":["bin/preview"]}
{"date":"2026-08-02","card":"i213","type":"pitfall","key":"current-memoized-store-stub-invisible-in-request","insight":"In this Rails 8.1 stack an integration request gets a fresh execution context, so Current.memory_store (and any singleton stub on a Memory::Store.current instance grabbed in the test thread) is invisible inside controllers — only Memory::Store.current = (the @given ivar) crosses the request boundary.","confidence":9,"files":["server/test/controllers/api/memory_controller_test.rb","server/app/services/memory/store.rb"]}
{"date":"2026-08-02","card":"i222","type":"pitfall","key":"desktop-specs-no-node-no-es2022","insight":"Now that `desktop/tsconfig.json` typechecks `test/**`, specs inherit `lib: [\"ES2020\", \"DOM\", \"DOM.Iterable\"]` and a package with no `@types/node`, so `Array.prototype.at()` and any `node:fs`/`node:path` import fail the gate — reach files via vite's `?raw` import instead.","confidence":9,"files":["desktop/tsconfig.json","desktop/package.json","desktop/test/rules.test.ts"]}
{"date":"2026-08-02","card":"i222","type":"pitfall","key":"vitest-message-arg-position","insight":"vitest reads an assertion's explanation from `expect(actual, message)`, not from a second argument to `toEqual`/`toBe` — six such messages in `rules.test.ts` had been silently dropped on failure until the widened type gate surfaced them.","confidence":8,"files":["desktop/test/rules.test.ts"]}
{"date":"2026-08-02","card":"i177","type":"operational","key":"openviking-contract-suite-runnable-locally","insight":"`Memory::OpenVikingTest` can be run for real against the repo's own `docker compose --profile memory` container by POSTing an isolated account to `/api/v1/admin/accounts` with `ov.conf`'s `root_api_key` and exporting the returned `user_key` as `OPENVIKING_API_KEY` — the write/list/supersede half of the contract then passes in ~0.3s, while the six `search`/retrieval cases fail with empty results whenever the container's embedding provider is down (visible as `openai_embedders ... Connection error` in `docker logs workroom-openviking-1`), so those failures must be read as environment, not regression.","confidence":9,"files":["server/test/services/memory/open_viking_test.rb","server/app/services/memory/open_viking.rb","docs/RUNNING.md"]}
{"date":"2026-08-02","card":"i177","type":"pattern","key":"preloader-after-last-not-includes","insight":"For a page built with `messages.order(:created_at).last(200)`, the nested polymorphic chain must be loaded with `ActiveRecord::Associations::Preloader.new(records:, associations: { author: { agent_session: :user } }).call` on the materialised array rather than with `includes` on the relation, because `last(n)` reverses and re-sorts the relation; the same shape works for memory entries via `Memory::Provenance.preload`, which skips the run-owner pass for entries that already name an author.","confidence":8,"files":["server/app/controllers/api/v1/channels_controller.rb","server/app/services/memory/provenance.rb"]}
{"date":"2026-08-03","card":"i177","type":"operational","key":"openviking-contract-needs-provisioned-account","insight":"Running the memory contract against a live OpenViking (`docker compose --profile memory`) needs an account key provisioned through `/api/v1/admin/accounts` — the config's `root_api_key` returns HTTP 403 on `/api/v1/content/write`, so a bare root key makes every contract case error rather than skip.","confidence":8,"files":["server/test/services/memory/open_viking_test.rb","server/app/services/memory/open_viking.rb"]}
{"date":"2026-08-03","card":"i177","type":"pitfall","key":"git-export-parameterizes-uri-tail","insight":"`RecordStore::GitExport#memory_key` parameterizes the uri tail, so the `~hex` suffix `Memory::Store#write_policy` gives a correction collapses to `-hex` in the git mirror — the mirror cannot distinguish `cadence~a1b2c3` from a title that parameterizes to `cadence-a1b2c3`, a collision surface that predates this card but now sits on the shared write path.","confidence":7,"files":["server/app/services/record_store/git_export.rb","server/app/services/memory/store.rb"]}
{"date":"2026-08-01","card":"i119","type":"pitfall","key":"store-contract-local-only-in-ci","insight":"`Memory::StoreContract` cases only actually execute against `Memory::Local` unless `OPENVIKING_URL`/`OPENVIKING_API_KEY` are set, so a plan that says a contract case covers `both stores` is verified for Local alone in a normal `bin/rails test` run.","confidence":9,"files":["server/test/services/memory/store_contract.rb","server/test/services/memory/open_viking_test.rb"]}
{"date":"2026-08-01","card":"i119","type":"pattern","key":"openviking-transport-swallows-errors","insight":"`Memory::OpenViking#read`/`list`/`mkdir`/`tag` each `rescue Error` and return nil/[], so a defect in a path that runs after `read` cannot be asserted with `assert_raises` — the derivation must happen before any HTTP call for a store-level error test to be red without a live store.","confidence":9,"files":["server/app/services/memory/open_viking.rb"]}
{"date":"2026-08-01","card":"i146","type":"pitfall","key":"memory-adapter-swallow-turns-down-into-empty","insight":"Widening `Memory::OpenViking#request`'s rescue makes every swallowing reader (`list` -> `[]`, `read` -> `nil`) convert an unreachable store into an empty result, so any endpoint built on `count`/`all` silently reports that the room knows nothing unless it also checks `available?`.","confidence":9,"files":["server/app/services/memory/open_viking.rb","server/app/controllers/api/v1/channels_controller.rb"]}
{"date":"2026-08-01","card":"i146","type":"tool","key":"openviking-tests-skip-without-live-store","insight":"`test/services/memory/open_viking_test.rb` skips its contract cases without `OPENVIKING_URL` (41 of 53 runs skipped), so a green run there proves little unless you check `--verbose` that the new cases actually executed.","confidence":9,"files":["server/test/services/memory/open_viking_test.rb","server/test/services/memory/store_contract.rb"]}
{"date":"2026-08-01","card":"i146","type":"pitfall","key":"openviking-list-read-swallow-errors","insight":"`Memory::OpenViking#list` and `#read` rescue the adapter's `Error` and return `[]`/`nil` by design, so any card that widens the transport rescue silently converts a 500 into a room that reads as empty at every listing call site (`all`, `count`, `skills`, and the memory/skills index endpoints) — the translation and the availability fact must land together at each of them.","confidence":9,"files":["server/app/services/memory/open_viking.rb","server/app/controllers/api/v1/memory_controller.rb","server/app/controllers/api/v1/skills_controller.rb"]}
{"date":"2026-08-01","card":"i146","type":"pitfall","key":"openviking-half-swallows-errors","insight":"`Memory::OpenViking` splits its own `Error` two ways — `list`, `read`, `mkdir` and `tag` swallow it and return empty, while `search`, `write`, `write_skill` and `supersede` let it escape to a 500 — so any store-availability work must cover both halves or it turns some call sites silently-empty and leaves the rest crashing.","confidence":9,"files":["server/app/services/memory/open_viking.rb","server/app/controllers/api/v1/memory_controller.rb"]}
{"date":"2026-08-01","card":"i146","type":"tool","key":"pr-body-gates-not-test-plan","insight":"This repo's CI check `pr-carries-evidence` requires a `## Gates` section in the PR body (CONTRIBUTING.md), not the workflow kit's `## Test plan`, so the acceptance check's literal `## Test plan` requirement contradicts the gate the project actually enforces.","confidence":9,"files":["CONTRIBUTING.md",".claude/prompts/acceptance-check.md"]}
{"date":"2026-08-01","card":"i127","type":"pitfall","key":"openviking-contract-skips-without-live-store","insight":"`Memory::OpenVikingTest` skips its entire `setup` when `OPENVIKING_URL` is unset, so any case a plan adds to `store_contract.rb` is executed against `Memory::Local` only — a plan that wants the adapter's own code exercised in CI must ask for a store-free unit test of the pure `serialize`/`deserialize` pair.","confidence":9,"files":["server/test/services/memory/open_viking_test.rb","server/test/services/memory/store_contract.rb"]}
{"date":"2026-08-01","card":"i127","type":"architecture","key":"provenance-requires-source-on-every-entry","insight":"`Memory::Provenance.of` calls `entry.source` first, so every store's Entry shape must define `source` (OpenViking's returns nil) or the memory index raises NoMethodError — a new store implementation now has one more member to satisfy than the `slice`-based reader contract implies.","confidence":8,"files":["server/app/services/memory/provenance.rb","server/app/services/memory/open_viking.rb"]}
{"date":"2026-08-03","card":"i280","type":"architecture","key":"run-record-write-vocabulary-lives-in-api-ts","insight":"The desktop client's run-record field names (context_used, context_size, cost, cost_currency, stop_reason) are written in api.ts (reportUsage, finishRun); main.ts only reads back presence broadcasts, so source-guard specs pinning that vocabulary must target api.ts.","confidence":9,"files":["desktop/src/api.ts","desktop/src/main.ts"]}
{"date":"2026-08-03","card":"i284","type":"pattern","key":"seeds-exit-zero-refusal","insight":"Environment guards in server/db/seeds.rb refuse by puts + bare exit, which terminates with status 0, so RAILS_ENV=test bin/rails db:prepare still exits 0 while leaving the database seedless (verified: users=0 workspaces=0 channels=0 memories=0 after prepare).","confidence":9,"files":["server/db/seeds.rb"]}
{"date":"2026-08-03","card":"i279","type":"pitfall","key":"self-scanning-source-tests-need-runtime-needles","insight":"Meta-tests in this repo that read their own containing file (lib.rs) must assemble grep needles at runtime (format!, a_fn helper) and strip comment lines before matching, or the test's own constants and doc comments satisfy/violate the assertions they encode.","confidence":8,"files":["desktop/src-tauri/src/lib.rs"]}
{"date":"2026-08-04","card":"i282","type":"pitfall","key":"move-card-baseline-checkout-contaminates","insight":"Measuring spec-count equality with `git checkout origin/main -- .` inside the card worktree double-counts moved specs because branch-new test files survive the checkout (362 vs the true 338) — take the baseline in a detached worktree at origin/main instead.","confidence":8,"files":["desktop/test/main.test.ts","desktop/test/rules.test.ts"]}
{"date":"2026-08-04","card":"i283","type":"pattern","key":"vite-ignore-dynamic-import-for-red-specs","insight":"Phase-A specs for a not-yet-existing desktop module must import it via a runtime-computed specifier marked @vite-ignore — a literal import fails the whole spec file at transform time and takes unrelated green specs down with it.","confidence":9,"files":["desktop/test/main.test.ts"]}
{"date":"2026-08-04","card":"i283","type":"pitfall","key":"rules-source-guards-pin-call-shape","insight":"rules.test.ts contains source-text guards that pin exact call-site shapes in main.ts (e.g. api.remember(current.slug), so an extraction that changes how a pinned call reads its arguments breaks specs outside the card's file list.","confidence":8,"files":["desktop/test/rules.test.ts","desktop/src/main.ts"]}
